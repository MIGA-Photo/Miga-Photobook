# تقرير: نظام Underpaid → Remaining Payment → Full Verification → Automatic Approval
### 15 سبتمبر 2026

هذا التقرير بالترتيب اللي طلبته بالظبط: (1) الملفات المتغيرة، (2) أهم
التعديلات، (3) أي migration، (4) أي متغيرات بيئة جديدة، (5) نتائج
الاختبارات، (6) النقاط اللي محتاجة اختبار حي. **لم يتم عمل أي نشر
(deployment) بعد — التنفيذ والاختبار خلصوا، والقرار في الرفع ليك.**

---

## 1) الملفات المتغيرة

| الملف | نوع التغيير |
|---|---|
| `backend/transform-worker.js` | التعديل الأساسي — كل منطق الدفع الجزئي الجديد |
| `index.html` | إضافة واجهة "الدفع الجزئي" في نافذة الشراء ونافذة التتبع |
| `admin-9k2x.html` | نفس إضافة `index.html` (مطابقة، فحص parity) |
| `app.js` | منطق الواجهة الجديد (عميل + أدمن) + نصوص عربي/إنجليزي جديدة |
| `en/index.html` | تولّد تلقائيًا من app.js (نفس البايبلاين المعتاد) — لم يُكتب يدويًا |
| `styles.css` | **لم يتغيّر** في هذه الدفعة |

---

## 2) أهم التعديلات

### أ) نموذج بيانات جديد لكل طلب — سجل دفعات تراكمي
كل طلب (`order`) بقى فيه:
- `payments[]` — سجل **تراكمي، لا يُمحى منه شيء أبدًا**: كل دفعة حقيقية
  اتأكدت (يدويًا من الأدمن أو webhook من بوابة دفع) بتضاف كسطر جديد،
  أبدًا مش بتستبدل السطر اللي قبلها. كل سطر فيه: `id` (مفتاح منع
  التكرار)، `provider`، `providerTransactionId`، `amount`، `method`،
  `reference`، `verifiedAt`، `verifiedBy`.
- `totalVerifiedPaid` — مجموع كل الدفعات في `payments[]`، محسوب من
  السيرفر دائمًا.
- `remainingAmount` — `max(0, requiredAmount - totalVerifiedPaid)`،
  محسوب من السيرفر دائمًا، **أبدًا مش من أي رقم يبعته المتصفح**.
- `overpaidAmount` — `max(0, totalVerifiedPaid - requiredAmount)` —
  للعلم فقط، لسه محتاج قرار منك (تفاصيل تحت في القسم 6).
- `pendingReferences[]` — أرقام عمليات تحويل جديدة بعتها العميل عن
  المبلغ المتبقي، لسه محتاجة مراجعة الأدمن (معلومة فقط، مش فلوس فعلية
  لحد ما الأدمن يتحقق منها).

`verifiedPaidAmount` القديم فضل موجود وبيتحدث تلقائيًا كـ"مرآة حية"
لـ`totalVerifiedPaid` — عشان بوابة `/transform` (اللي بتقرر تسمح
بالتحويل ولا لأ) **لم تتغيّر إطلاقًا** ولسه بتقرأ نفس الحقل القديم.

### ب) `recordPaymentVerification` — أعيد بناؤها بالكامل
كل دفعة جديدة لازم يكون معاها **مفتاح منع تكرار فريد**:
`provider:providerTransactionId` (لبوابات الدفع) أو `paymentId` (للأدمن
اليدوي). لو نفس المفتاح جه تاني، بيتم تجاهله بأمان (`duplicate: true`)
من غير ما يُحتسب مرتين. الدالة القديمة كانت بترفض أي محاولة تانية لو
الطلب "متأكد بالفعل" — وده كان بيضيّع أي دفعة إضافية حقيقية (زي حالة
دفع زيادة عن السهو). دلوقتي كل دفعة فريدة بتتسجل، والموافقة النهائية
(`status = approved`) بتحصل بس لما المجموع التراكمي يوصل للمطلوب.

### ج) نقطة نهاية جديدة للعميل — `POST /orders/submit-remaining-payment`
العميل يقدر يبعت رقم عملية تحويل جديد عن **نفس كود الطلب**، من غير ما
يعمل طلب جديد خالص. مفيش أي مبلغ بيتقبل من العميل هنا — بس رقم العملية،
واللي بيقرر المبلغ الفعلي هو الأدمن (زي أول عملية بالظبط).

### د) `approveOrder` (الموافقة اليدوية) — بقت تتطلب `paymentId`
لازم تبعت `paymentId` فريد مع كل موافقة (واجهة الأدمن بتولّده تلقائيًا
وبتحتفظ بيه لحد ما يتغيّر المبلغ أو تتحدّث الصفحة) — عشان لو حصل خطأ
شبكة وحاول الأدمن يوافق تاني على نفس الدفعة، متتحسبش مرتين. تقدر كمان
تبعت `reference` تحدد رقم عملية معيّن (من `pendingReferences`) لو فيه
أكتر من رقم عملية معلّق.

### هـ) بوابات الدفع (Paymob/Fawry/Fawaterk) — لم تعد تثق بالمبلغ من العميل
`paymobCreate`/`fawryCreate`/`fawaterkCreate` كانت بتاخد `amount` من
جسم الطلب مباشرة وتبعته للبوابة زي ما هو. دلوقتي بتتجاهل أي مبلغ من
العميل تمامًا، وبتحسب `remainingAmount` بنفسها من السيرفر، وده اللي
بيتبعت فعليًا للبوابة. لو الطلب مدفوع بالكامل، بترفض إنشاء أي عملية
دفع جديدة (409).

### و) Fawaterk — تصحيح حقيقي مبني على التوثيق الرسمي، مش تخمين
كنت حذّرت في تقرير حماية الدفع السابق إن حقل المبلغ في webhook فوتوباك
كان تخمين. رجعت لتوثيق Fawaterk الرسمي (`fawaterak-api.readme.io`) ولقيت
إن الـ webhook **مفيهوش أي حقل مبلغ إطلاقًا** — الحقول الموثقة فقط هي:
`hashKey`, `invoice_key`, `invoice_id`, `payment_method`,
`invoice_status`, `pay_load`, `referenceNumber`. فبدل ما أخمّن اسم
الحقل، الكود دلوقتي — بعد ما يتأكد من التوقيع (HMAC) — بيعمل نداء تاني
لـ API الرسمي لفوتوباك (`GET /api/v2/getInvoiceData/{invoice_id}`،
موثّق برضه رسميًا) وياخد المبلغ الحقيقي (`total`) وحالة الدفع الفعلية
(`paid: 1/0`) منه مباشرة. هذا أدق وأأمن من أي تخمين لاسم حقل.

---

## 3) هل فيه Data Migration مطلوب؟
**لا — لا يوجد أي migration أو سكريبت تعديل بيانات مطلوب.**

الطلبات القديمة (اللي اتعملت قبل هذا التحديث) هتفضل شغالة بنفس
`status`/`paymentStatus` بتاعتها بالظبط، لأن بوابة `/transform` لسه
بتقرأ نفس الحقول القديمة زي ما هي. أول مرة أي طلب قديم يتعامل معاه
`recordPaymentVerification` تاني (مثلاً لو حصل فيه دفعة إضافية)، هيتولّد
له تلقائيًا سطر واحد في `payments[]` من بياناته القديمة (`verifiedPaidAmount`
الخ) — من غير ما تحتاج تشغّل أي سكريبت بنفسك.

## 4) هل فيه متغيرات بيئة (Environment Variables) جديدة؟
**لا — لا يوجد أي متغير بيئة جديد.** كل الأسرار المستخدمة (Paymob،
Fawry، `FAWATERK_API_KEY`، `FAWATERK_VENDOR_KEY`) موجودة بالفعل من قبل.
الميزة الجديدة بتستخدم نفس `FAWATERK_API_KEY` الموجود لعمل نداء إضافي
لـ API التحقق من الفاتورة.

---

## 5) نتائج الاختبارات

كل الاختبارات دي آلية (كود حقيقي بيشغّل نسخة من الـ Worker الجديد على
بيانات وهمية في الذاكرة — مفيش أي طلب حقيقي أو webhook مزيّف اتبعت
لأي بوابة دفع حقيقية أو لـ Production).

**86 من 86 اختبار ناجح (0 فشل):**

- **35/35** — نفس مجموعة اختبارات "حماية الدفع" الأصلية (رفعة الصباح)،
  أعيد تشغيلها بالكامل بعد التعديلات للتأكد إن حماية السعر/الباقات/
  الاشتراكات والموافقة اليدوية لسه شغالة صح 100%.
- **8/8** — اختبارات الـ webhooks الحقيقية (Paymob/Fawry/Fawaterk) بتوقيع
  محسوب فعليًا، شاملة تصحيح Fawaterk الجديد (محاكاة API التحقق من
  الفاتورة بدل تخمين حقل).
- **43/43** — اختبارات السيناريوهات الـ11 اللي حددتها بالظبط، بنفس
  الأرقام (259 / 200 / 59):

| # | السيناريو | النتيجة |
|---|---|---|
| 1 | دفع 200 من 259 → underpaid, متبقي 59, /transform مرفوض | ✅ |
| 2 | دفعة تانية 59 على **نفس الكود** → مكتمل 259، /transform مسموح | ✅ |
| 3 | 200 + 50 = 250 من 259 → لسه underpaid, متبقي 9 | ✅ |
| 4 | نفس webhook مكرر مرتين → يُحتسب مرة واحدة فقط | ✅ |
| 5 | webhook مكرر بعد دفعتين حقيقيتين → المجموع يفضل 259 | ✅ |
| 6 | العميل يحاول التلاعب بالمبلغ في الفرونت إند (Paymob/Fawry/Fawaterk) | ✅ مرفوض، يُستخدم المبلغ المحسوب سيرفريًا فقط |
| 7 | التلاعب بالسعر الأساسي (تأكيد الحماية الموجودة أصلًا) | ✅ |
| 8 | نفس رقم عملية دفع يُستخدم لطلبين مختلفين | ✅ مرفوض للطلب الثاني |
| 9 | محاولة وصول/تعديل غير مصرح به (كود غير موجود + حقول محقونة في body) | ✅ مرفوض/متجاهل |
| 10 | تحديث الصفحة/الإغلاق/العودة لاحقًا — الحالة تفضل صحيحة من السيرفر | ✅ |
| 11 | دفع أكتر من المطلوب (200+100=300 من 259) — يُسجَّل بأمان، بدون رد أموال مُخترع | ✅ overpaidAmount=41 محسوب ومعروض |

بالإضافة لفحص برمجي (grep-level) تأكيد إن مفيش أي endpoint بيقبل
`requiredAmount`/`totalVerifiedPaid`/`remainingAmount`/`paymentStatus`
من جسم الطلب مباشرة (البنود 1-4 من قائمة الأمان).

### واجهة المستخدم (Playwright، محليًا)
27/27 فحص ناجح على الصفحة العربية والإنجليزية ولوحة الأدمن: الرسالة
الجديدة بتظهر بالضبط زي ما طلبت (بدون كلمة تقنية "UNDERPAID")، زر "دفع
المبلغ المتبقي" بيوضح المبلغ الصحيح، إرسال رقم العملية بيشتغل، ورسالة
الاكتمال بتظهر بعد إتمام الدفع — كل ده بدون أي خطأ JavaScript.

### فحص الموقع العام (audit-site.py)
358/374 — **نفس النتيجة بالظبط** قبل وبعد هذا التعديل. الـ16 فحص
الفاشل قديمة تمامًا (تصميم اللوجو، الألوان، التفاف اسم الموقع، صورة
الأفاتار) ومش لها علاقة بهذه الدفعة إطلاقًا.

---

## 6) نقاط لم يتم اختبارها حيًّا (تحتاج تأكيدك)

1. **الحماية من التكرار عند طلبين متزامنين حرفيًا (race condition)
   داخل بنية Cloudflare Workers KV الحالية:** الحماية الحالية (مفتاح
   `providertxn:` + مفتاح فريد لكل دفعة) بتمنع بأمان تام أي webhook
   متكرر يوصل **بالتتابع** (إعادة إرسال بعد Timeout، دبل-كليك من
   الأدمن) — وهو السيناريو الواقعي الشائع. لكن Workers KV (بدون
   Durable Objects أو D1 transactions) مفيهوش قفل ذري حقيقي، فلو
   وصل **نفس الـ webhook بالظبط في نفس اللحظة الفعلية** (تزامن كامل،
   مش تكرار متتابع)، فيه احتمال نظري ضئيل جدًا إن الاثنين يقروا
   الطلب قبل ما أي منهم يكتب، ويتسجلوا مرتين. هذا قيد معروف في أي
   نظام مبني على KV بدون transactions حقيقية — "قدر الإمكان ضمن بنية
   KV الحالية" زي ما طلبت بالظبط، لكن مش حماية 100% مطلقة ضد هذا
   السيناريو النادر جدًا. لو عايز إغلاق كامل لهذه الثغرة النظرية،
   محتاجين Durable Object واحد لكل طلب — ده تغيير معماري أكبر، قولّي
   لو عايزني أنفذه في دفعة منفصلة.
2. **Fawaterk Live Validation:** التصحيح الجديد مبني على التوثيق
   الرسمي (مش تخمين)، ومُختبَر بمحاكاة كاملة لتلك الـ API. لكن
   الدمج الحقيقي مع Fawaterk (باقي معطّل من الفرونت إند حسب علمي من
   قبل) لسه محتاج معاملة حقيقية واحدة صغيرة قبل التفعيل الكامل، بالظبط
   زي ما نبّهتك في التقرير السابق. **لم أرسل ولن أرسل أي webhook مزيّف
   لأي بوابة حقيقية أو لـ Production.**
3. **اختبار على الموقع الحي بعد الرفع:** القيود التقنية لهذه الجلسة (لا
   يوجد وصول مباشر لموقعك الحي من هنا) لسه قائمة زي كل مرة — التحقق من
   `miga-photobook.com` نفسه بعد الرفع محتاج منك تجربة حقيقية (أو
   تطلب مني أعمل تدقيق Cloudflare Production زي المرة اللي فاتت بعد
   ما ترفع).

---

## الخلاصة والخطوة التالية

التنفيذ والاختبار خلصوا بالكامل، و**86/86 اختبار آلي + 27/27 فحص واجهة
نجحوا**. **لم يتم عمل أي نشر (deployment) من هذه الجلسة** — لا توجد أداة
نشر مباشر لـ Cloudflare متاحة هنا، ولا صلاحية رفع مباشر على GitHub (نفس
القيد الموثّق من رفعة حماية الدفع الأولى النهاردة). الملفات جاهزة
للرفع اليدوي (تفاصيل الطريقة في `اقرأني-قبل-الرفع.txt`).

بعد ما ترفع، قولّي وهعمل تدقيق كامل من Cloudflare Production وGitHub
تاني (زي التدقيق اللي عملته قبل كده) للتأكد إن كل حاجة شغالة صح على
الموقع الحي فعليًا.
